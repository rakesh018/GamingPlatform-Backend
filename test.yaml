config:
  target: http://localhost:3000
  phases:
    - duration: 30
      arrivalRate: 20
      name: Spike phase
scenarios:
  - flow:
      - post:
          url: "/bets/makeBet"
          headers:
            Authorization: "Bearer "
          json:
            gameName: "coinFlip"
            roundDuration: 1
            betAmount: 10
            choice: "tail"
            
